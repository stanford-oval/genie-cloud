#!/bin/bash

case $DOCKER_TAG in)
	*-cuda)
		docker build --build-arg BASE_IMAGE=stanfordoval/decanlp:latest-cuda -f $DOCKERFILE_PATH -t $IMAGE_NAME .
	;;

	*)
		docker build -f $DOCKERFILE_PATH -t $IMAGE_NAME .
	;;
esac
