extends layout

block page_name
  - index = true;
  | Install App

block content
  div.jumbotron.text-center
    h1 You already installed this app!

    p And it seems to be running fine.

    p
      a(href='/apps/' + appId + '/show')