apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

bases:
  - ../base

patches:
  - job.yaml
  # NOTE  This file must be created locally, see adjacent
  #       `job.local.yaml.EXAMPLE` file
  - job.local.yaml
