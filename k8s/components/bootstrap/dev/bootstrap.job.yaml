apiVersion: batch/v1
kind: Job
metadata:
  name: bootstrap
spec:
  template:
    spec:
      containers:
        - name: main
          imagePullPolicy: IfNotPresent
          env:
            - name: NODE_ENV
              value: development
