thingpedia_name: Remote Almond
thingpedia_description: A proxy device for a Almond owned by a different user. This device is created and managed automatically by the system.
repository: https://github.com/stanford-oval/thingengine-core
subcategory: service

class: |
  class @org.thingpedia.builtin.thingengine.remote
  #[system=true]
  {
    import loader from @org.thingpedia.builtin();
    import config from @org.thingpedia.config.builtin();
  }

dataset: |
  dataset @org.thingpedia.builtin.thingengine.remote language "en" {
  }
